<!--pages/home-video/index.wxml-->
<!-- 使用 wx:for-item 可以指定数组当前元素的变量名，默认item
     使用 wx:for-index 可以指定数组当前下标的变量名, 默认index
 -->

<view class="video">
  <view class="item" wx:for="{{topMVs}}" wx:key="id">
    <video-item-v1 item="{{item}}"></video-item-v1>
  </view>
</view>
